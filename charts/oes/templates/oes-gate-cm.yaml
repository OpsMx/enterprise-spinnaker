apiVersion: v1
data:
  gate-local.yml: |
    {{- tpl (.Files.Get "config/gate-local.yml") . | nindent 4 }}

  gate.yml: |
    {{- tpl (.Files.Get "config/gate.yml") . | nindent 4 }}

  spinnaker.yml: |
    {{- tpl (.Files.Get "config/spinnaker.yaml") . | nindent 4 }}

  spinnakerconfig.yml: |
    {{- tpl (.Files.Get "config/spinnakerconfig.yml") . | nindent 4 }}

  spinnakerconfig.yml.vault: |
    {{- tpl (.Files.Get "config/spinnakerconfig.yml.vault") . | nindent 4 }}

kind: ConfigMap
metadata:
  name: oes-gate-config
