apiVersion: v1
data:
  audit.yml: |
    {{- tpl (.Files.Get "config/audit.yml") . | nindent 4 }}

  persistence.yml: |
    {{- tpl (.Files.Get "config/persistence.yml") . | nindent 4 }}

  spinnaker.yml: |
    {{- tpl (.Files.Get "config/spinnaker-server.yaml") . | nindent 4 }}

kind: ConfigMap
metadata:
  name: sapor-config
