{{- if and (.Values.enableCentralMonitoring) (.Values.global.grafana.enabled) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
{{- if .Values.global.certManager.installed }}
    acme.cert-manager.io/http01-edit-in-place: "true"
    cert-manager.io/issue-temporary-certificate: "true"
    cert-manager.io/issuer: letsencrypt-{{ .Release.Namespace }}-oes
    kubernetes.io/tls-acme: "true"
{{- end }}
{{- if .Values.global.grafana.ingress.annotations }}
{{ tpl (toYaml .Values.global.grafana.ingress.annotations) . | indent 4 }}
{{- end }}
  name: grafana-ingress
spec:
  rules:
  - host: {{ .Values.global.grafana.host }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Release.Name }}-grafana
            port:
              number: 80
        path: /
        pathType: ImplementationSpecific
{{- if .Values.global.ssl.enabled }}
  tls:
  - hosts:
    - {{ .Values.global.grafana.host }}
{{ toYaml .Values.global.grafana.ingress.tls | indent 4 }}
{{- end }}
{{- end }}