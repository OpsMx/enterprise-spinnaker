apiVersion: v1
kind: Service
metadata:
  labels:
    app: oes-server
  name: oes-server-svc
spec:
  type: {{ .Values.k8sServiceType }}
  ports:
  - name: "oes-server"
    port: 8085
    targetPort: 8085
  selector:
    app: oes-server
