{{- if .Values.global.HAstormdriver }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: stormdriver-caching
  name: stormdriver-caching
spec:
  ipFamilies:
    - IPv4
  ipFamilyPolicy: SingleStack
  ports:
    - name: http
      port: 8090
      protocol: TCP
      targetPort: 8090
  selector:
    app: stormdriver-caching
  sessionAffinity: None
  type: ClusterIP
{{- end }}
