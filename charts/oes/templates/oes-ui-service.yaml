apiVersion: v1
kind: Service
metadata:
  labels:
    app: oes-ui
  name: oes-ui-svc
spec:
  type: {{ .Values.k8sServiceType }}
  ports:
  - name: "oes-ui-service"
    port: 80
    targetPort: 80
  selector:
    app: oes-ui
