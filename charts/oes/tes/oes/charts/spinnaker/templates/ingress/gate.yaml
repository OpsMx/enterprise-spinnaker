---
# Source: oes/charts/spinnaker/templates/ingress/gate.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: RELEASE-NAME-spinnaker-gate
  labels:
    app: "RELEASE-NAME-spinnaker"
    heritage: "Helm"
    release: "RELEASE-NAME"
    chart: "spinnaker-2.2.3"
spec:
  rules:
  - host: "gate.spinnaker.example.org"
    http:
      paths:
      - path: /
        backend:
          serviceName: spin-gate
          servicePort: 8084
