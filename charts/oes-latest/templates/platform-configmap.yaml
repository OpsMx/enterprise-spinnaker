apiVersion: v1
data:
  application.properties: |
    {{- tpl (.Files.Get "config/application.properties") . | nindent 4 }}

kind: ConfigMap
metadata:
  name: platformconfig
  labels:
{{ include "autopilot.standard-labels" . | indent 4 }}
