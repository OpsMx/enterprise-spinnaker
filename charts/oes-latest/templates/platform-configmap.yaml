apiVersion: v1
data:
  application.yml: |
    {{- tpl (.Files.Get "config/application.yml") . | nindent 4 }}

  userGroups.yml: |
    {{- tpl (.Files.Get "config/userGroups.yml") . | nindent 4 }}

kind: ConfigMap
metadata:
  name: platformconfig
  labels:
{{ include "autopilot.standard-labels" . | indent 4 }}
