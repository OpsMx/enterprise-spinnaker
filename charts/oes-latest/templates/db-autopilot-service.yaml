apiVersion: v1
kind: Service
metadata:
  labels:
    app: db-autopilot
{{ include "autopilot.standard-labels" . | indent 4 }}
  name: db-autopilot
spec:
  type: ClusterIP
  ports:
  - name: db-autopilot
    protocol: TCP
    port: 5432
    targetPort: 5432
  selector:
    app: db-autopilot
