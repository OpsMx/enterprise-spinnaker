apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: db-opsmx
{{ include "autopilot.standard-labels" . | indent 4 }}
  name: db-opsmx
spec:
  podManagementPolicy: {{ .Values.oesdb.podManagementPolicy }}
  replicas: 1
  serviceName: db-opsmx
  selector:
    matchLabels:
      app: db-opsmx
  template:
    metadata:
      labels:
        app: db-opsmx
{{ include "autopilot.standard-labels" . | indent 8 }}
    spec:
      imagePullSecrets:
        - name: opsmx11-secret
      containers:
      - image: {{ template "oesdb.image" . }}
        imagePullPolicy: {{ .Values.oesdb.image.pullPolicy }}
        name: db-opsmx
        ports:
        - containerPort: 5432
          protocol: TCP
        volumeMounts:
        - mountPath: "/var/lib/pgsql-pv"
          name: db-opsmx-postgresql
        readinessProbe:
          tcpSocket:
            port: 5432
          initialDelaySeconds: 10
          periodSeconds: 5
      securityContext:
        fsGroup: {{ .Values.oesdb.securityContext.fsGroup }}
  volumeClaimTemplates:
  - metadata:
      creationTimestamp: null
      labels:
        app: db-opsmx
      name: db-opsmx-postgresql
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: {{ .Values.oesdb.storageMountSize }}
      volumeMode: Filesystem
