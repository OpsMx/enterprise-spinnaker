apiVersion: v1
kind: Service
metadata:
  labels:
    app: db-sapor
{{ include "oes.standard-labels" . | indent 4 }}
  name: db-sapor
spec:
  type: ClusterIP
  ports:
  - name: db-sapor
    protocol: TCP
    port: 5432
    targetPort: 5432
  selector:
    app: db-sapor
